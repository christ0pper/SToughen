node_modules/
.next/
out/
*.log
.env
.env.local
# --- databases ---------------------------------------------------------------
# Payroll data lives in Supabase. No database file belongs in this repo: a local
# copy or backup holds names, pay and password hashes. A SQLite -wal file was
# pushed by mistake once, back when the app ran on SQLite.
*.db
*.db-journal
*.db-wal
*.db-shm
*.sqlite
*.sqlite3
*.dump
*.sql.gz
uploads/
coverage/
*.tsbuildinfo
