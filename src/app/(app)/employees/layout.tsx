/** Holds the parallel slot the intercepted employee dialog renders into. */
export default function EmployeesLayout({
  children,
  modal,
}: {
  children: React.ReactNode;
  modal: React.ReactNode;
}) {
  return (
    <>
      {children}
      {modal}
    </>
  );
}
